export const metadata = {
  title: "School Gallery | PM International School",
  description: "Take a visual tour of PM International School. Explore images of our campus, events, classrooms, and student activities.",
};

export default function GalleryLayout({ children }) {
  return <>{children}</>;
}
